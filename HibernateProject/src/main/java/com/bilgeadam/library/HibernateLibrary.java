package com.bilgeadam.library;

public class HibernateLibrary {
	/*
	 * Sıklıkla kullandığımız annotation
	 * @Entity
	 * @Table
	 * @Id
	 * @GeneratedValue
	 * @Column
	 * @Temporal
	 * @Lob
	 * @Transient
	 * @Version
	 * ...
	 */
	
	/*
	 * persistence:kalıcılık
	 * ORM: Object Relating Mapping
	 * JPA:Java Persistence API
	 * JPA:Specification yani yapılar bunun üzerine inşaa ediliyor
	 * Hibernate:Hem java hem .NET
	 */
	
	/*
	 * JPA:Java Persistence API
	 * JPA:Specification yani yapılar bunun üzerine inşaa ediliyor
	 * JPA:Tek başına bir şey yapamaz bunun yardımncısı olması gerekiyor
	 * Örneğin: Hibernate
	 * JPA Hibernate'in super setidir.
	 * JPQL
	 */
	
	/*
	 * JPA implementation
	 * Hibernate
	 * EclipseLink
	 * Open
	 */
	
	/*
	 * Transaction:
	 * getTransaction().begin()
	 * kodlar
	 * getTransaction().commit()
	 * Create+
	 * Delete+
	 * Update+
	 * Select - ve ya +
	 */
	
	/*
	 * Dev+Ops:Docker+Jenkins+
	 * Virtual Box VM
	 * Eclipse Database erişmek
	 * JWT
	 */
}
